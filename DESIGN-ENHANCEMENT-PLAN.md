# LUMINA 디자인 고도화 개발 계획

> 작성일: 2026-08-21
> 승인 콘셉트: **오리엔탈 판타지 — 살아 있는 운명 도감**
> 범위: 홈, 사주, 타로, 수비학, 성향검사, 운세, 공통 결과 화면
> 목표: 이미지와 애니메이션을 풍부하게 사용하되 가독성, 접근성, 모바일 성능을 훼손하지 않는 상용 수준의 몰입형 경험 구축

## 1. 계획의 결론

LUMINA는 현재의 먹빛·상감·한자 중심 정체성을 버리지 않고, 사용자가 다섯 개의 세계를 탐험하는 **살아 있는 운명 도감**으로 확장한다. 홈에서는 각 기능이 풀컬러 삽화가 있는 포털이 되고, 기능 안에서는 입력과 선택이 하나의 짧은 의식처럼 진행되며, 결과 화면에서는 계산 과정과 상징이 시각적으로 조립된다.

애니메이션은 많되 동시에 소란스럽게 재생하지 않는다. 화면 전체에 흐르는 저강도 환경 모션, 사용자의 행동에 즉시 반응하는 마이크로인터랙션, 결과가 등장할 때만 사용하는 짧은 시네마틱 연출의 세 층으로 나눈다. 한 장면에서 가장 중요한 요소 하나만 강하게 움직이게 하여 재미와 집중을 함께 확보한다.

## 2. 현재 디자인 진단

### 2-1. 이미 확보된 강점

- `src/app/globals.css`에 먹빛 배경, 오행 색상, 한글·한자·수치용 서체, 스크롤 리빌, 카드 플립, 정령 등장 등 기본 시각 문법이 있다.
- `InkField`, `Reveal`, `Tilt`, `PillarGrid`가 CSS 모션과 작은 Client Component를 조합하는 안전한 패턴을 이미 보여 준다.
- 타로 78장, 사주 십이지 12장, 별자리 12장, 수비학 12장, 성향 요인 5장 등 총 119개 주제의 풀컬러 원화가 있다.
- 각 원화는 PNG·WebP·AVIF로 준비되어 있고 화면은 `next/image`와 WebP 소스를 사용한다.
- `prefers-reduced-motion`, 키보드 포커스, 서버 HTML 가시성, 인쇄 모드가 이미 고려되어 있다.
- 홈에서 기능을 먼저 선택한 뒤 사주 입력을 공개하는 점진적 공개 구조가 구현되어 있다.

### 2-2. 고도화가 필요한 지점

- 홈의 다섯 기능 카드가 동일한 구조와 무채색 표현을 사용해 각 기능의 세계관이 첫 화면에서 드러나지 않는다.
- 대부분의 랜딩 페이지가 동일한 헤더·제목·구분선·폼 패턴을 반복해 탐험하는 느낌보다 문서를 읽는 느낌이 강하다.
- 풍부한 이미지가 주로 결과 화면 일부에만 나타나므로 사용자가 기능을 선택하기 전에 받을 시각적 보상이 적다.
- 효과가 개별 컴포넌트에 분산되어 있어 지속시간, 이징, 깊이, 광원, 강도를 일관되게 제어하는 모션 토큰이 없다.
- 현재 정적 자산은 119개 원화 세트, 357개 파일로 약 416MB이며 이 중 PNG 원본이 약 396MB다. 실제 전송은 최적화 포맷을 사용하더라도 배포 자산 관리와 캐시 전략이 필요하다.
- 어두운 배경과 작은 보조 글자가 겹치는 구간은 화려한 효과가 추가될수록 다시 가독성이 낮아질 위험이 있다.

## 3. 브레인스토밍 접근안과 선택 이유

| 접근안 | 장점 | 위험 | 판단 |
| --- | --- | --- | --- |
| 오리엔탈 판타지 | 기존 먹빛 정체성을 유지하면서 이미지·광원·의식형 전환을 자연스럽게 확장할 수 있다. | 과한 어둠과 장식이 본문을 덮을 수 있다. | **채택** — 밝은 콘텐츠 패널과 모션 예산으로 보완한다. |
| 점술 아케이드 | 수집, 뱃지, 랜덤 이벤트 등 즉각적인 재미가 강하다. | 문화적·과학적 신뢰 계층이 가벼워 보일 수 있다. | 일부 발견 요소만 후순위 실험으로 사용한다. |
| 프리미엄 에디토리얼 | 대형 이미지와 절제된 모션으로 신뢰감과 고급감이 높다. | 사용자가 직접 만지는 재미가 약하다. | 결과 리포트의 조판과 공유 카드에 부분 적용한다. |

## 4. 경험 설계 원칙

### 4-1. 한 화면, 한 주인공

배경 입자, 카드, 텍스트, 장식이 동시에 강하게 움직이지 않는다. 환경 모션은 느리고 약하게 유지하고, 사용자가 선택하거나 결과가 생성될 때만 해당 대상이 전면으로 나온다.

### 4-2. 기능마다 다른 세계, 제품 전체는 같은 문법

- 사주: 먹, 석판, 오행의 빛, 십이지 정령
- 타로: 벨벳 테이블, 금박 카드, 촛불과 별가루
- 수비학: 숫자 성소, 기하학, 빛의 파동
- 성향검사: 투명 계측기, 심상 정원, 성장하는 형태
- 운세: 천구의, 새벽 하늘, 별자리 궤적

세계는 다르지만 테두리 두께, 서체, 콘텐츠 패널, 포커스 링, 버튼 크기, 모션 이징은 공통 토큰을 사용한다.

### 4-3. 이미지는 장식이 아니라 선택의 단서

이미지는 기능 카드의 분위기, 선택 결과, 진행 상태를 설명해야 한다. 의미 없는 배경 이미지를 여러 장 겹치는 대신 대표 이미지 한 장과 가벼운 광원·마스크·입자 레이어를 조합한다.

### 4-4. 재미와 신뢰 계층을 분리

문화적 해석과 엔터테인먼트 화면에는 판타지 연출을 적극 사용한다. 과학적 계층인 성향검사는 응답과 결과의 정확성을 흐리지 않도록 모션을 진행 피드백과 추상 시각화에만 사용한다.

### 4-5. 움직임이 없어도 완성된 화면

모든 핵심 정보와 행동은 애니메이션 없이도 보이고 사용할 수 있어야 한다. 모션은 이해와 즐거움을 더하는 층이며 콘텐츠의 유일한 전달 수단이 아니다.

## 5. 공통 시각 시스템

### 5-1. 밝기와 표면 체계

현재 단일한 어두운 배경 위에 다음 네 표면을 정의한다.

- `canvas`: 기능별 환경 이미지와 광원이 놓이는 가장 뒤 레이어
- `stage`: 대표 상징과 인터랙션이 놓이는 중간 레이어
- `reading-panel`: 본문을 읽는 밝고 불투명도 높은 패널
- `control`: 버튼, 탭, 입력처럼 명확한 대비가 필요한 최상위 레이어

본문 패널은 이미지가 밝거나 복잡해도 텍스트 대비가 유지되도록 독립된 배경색과 블러 상한을 가진다. 긴 결과 본문은 장식 배경 위에 직접 올리지 않는다.

### 5-2. 모션 토큰

| 토큰 | 용도 | 권장 범위 |
| --- | --- | --- |
| 즉시 반응 | 누름, 선택, 토글, 포커스 | 100~180ms |
| 상태 전환 | 카드 확대, 탭 변경, 폼 공개 | 240~480ms |
| 장면 전환 | 뽑기, 결과 조립, 포털 진입 | 700~1,400ms |
| 환경 모션 | 안개, 광원, 별, 먹 번짐 | 18~70초 반복 |

공통 이징, 거리, 회전각, 블러 상한, 3D 깊이를 CSS 변수 또는 타입이 있는 상수로 정의한다. 페이지마다 임의의 숫자를 추가하지 않는다.

### 5-3. 공통 컴포넌트 후보

| 컴포넌트 | 역할 | 경계 |
| --- | --- | --- |
| `SceneShell` | 기능별 배경, 광원, 콘텐츠 패널의 공통 골격 | Server Component |
| `FeaturePortal` | 홈의 대형 기능 포털과 대체 정적 상태 | Server Component 중심 |
| `PortalMotion` | 포인터 깊이, 스크롤 진입, 터치 선택 반응 | 작은 Client Component |
| `AmbientLayer` | CSS 기반 먹, 별, 광원, 부유 문양 | Server Component 또는 CSS 전용 |
| `InteractiveStage` | 제스처 좌표와 현재 선택 상태만 관리 | Client Component |
| `ResultCover` | 기능별 대표 이미지, 제목, 핵심 문장 | Server Component |
| `MotionSafeImage` | 이미지 진입, 저동작 대체, 로딩 상태 통일 | 필요 최소 범위의 Client Component |
| `ChapterNav` | 긴 결과의 현재 위치와 빠른 이동 | Client Component |

전역 Provider로 전체 앱을 Client Component화하지 않는다. 정적 레이아웃, 번역, 결과 데이터 조합은 서버에 두고 상호작용이 필요한 장면만 직렬화 가능한 View Model을 전달받는다.

## 6. 화면별 이미지·애니메이션 아이디어

### 6-1. 홈 — 다섯 세계의 현관

| 아이디어 | 이미지 요소 | 애니메이션·재미 요소 | 우선순위 |
| --- | --- | --- | --- |
| 대형 기능 포털 | 각 기능의 대표 원화 콜라주 | 진입 시 문이 열리듯 마스크 확장 | P0 |
| 현재의 하늘 | 실시간 사주 여덟 글자와 천구 배경 | 글자 부유, 광원 이동, 시간 변화 | P0 |
| 포털 미리보기 | 사주 정령·타로 카드·숫자·정원·별자리 | hover/focus 시 대표 상징 한 동작 재생 | P0 |
| 스크롤 세계 전환 | 먹 구름과 금빛 선 | 포털 사이에서 배경 색온도 변화 | P1 |
| 오늘의 한 문장 | 작은 삽화 프레임 | 봉인이 열리며 문장 공개 | P1 |
| 숨은 인장 | 배경에 낮은 대비의 문양 | 발견 시 짧은 빛 번짐, 로컬 기록 | P2 |

홈의 첫 뷰포트는 한 장의 대표 장면만 우선 로드한다. 나머지 포털 이미지는 뷰포트 접근 시 로드하고, 모바일에서는 포인터 패럴랙스 대신 탭·스크롤 반응으로 대체한다.

### 6-2. 사주 — 시간의 문과 오행 정령

| 단계 | 이미지 요소 | 애니메이션·재미 요소 | 우선순위 |
| --- | --- | --- | --- |
| 출생일 입력 | 해·달·절기 고리 | 날짜 변경 시 고리가 짧게 회전 | P1 |
| 출생시 입력 | 십이지 시간 다이얼 | 시간대에 맞는 동물이 빛남 | P1 |
| 장소 선택 | 동아시아 지도 질감 | 선택 도시의 빛점과 경도선 표시 | P2 |
| 계산 전환 | 먹으로 그린 문 | 제출 시 여덟 글자가 문 안으로 모임 | P0 |
| 원국 결과 | 네 석판과 십이지 원화 | 연→월→일→시 순서로 기둥 조립 | P0 |
| 오행 분포 | 다섯 색의 정령·원형 차트 | 비율에 따라 정령의 크기와 호흡 변화 | P0 |
| 관계 해석 | 합·충·형·해 연결선 | 해당 설명을 읽을 때 관계선 점등 | P1 |
| 대운·연운 | 산수화형 시간 풍경 | 스크롤 또는 드래그로 시대 이동 | P1 |

### 6-3. 타로 — 직접 펼치고 뽑는 카드 의식

| 아이디어 | 이미지 요소 | 애니메이션·재미 요소 | 우선순위 |
| --- | --- | --- | --- |
| 스프레드 무대 | 벨벳·목재·별자리 테이블 | 선택한 배열에 맞춰 안내선 등장 | P0 |
| 카드 더미 | 전용 공통 카드 뒷면 | 부채꼴 펼침, 섞기, 다시 모으기 | P0 |
| 직접 뽑기 | 78장 원화의 뒷면 상태 | 클릭·탭·드래그로 카드 선택 | P0 |
| 공개 순간 | 풀컬러 카드 원화 | 깊이감 있는 플립과 짧은 광원 파동 | P0 |
| 정·역방향 | 카드 원화와 방향 인장 | 회전 후 방향 표식이 별도로 고정 | P0 |
| 해석 집중 | 카드별 확대 장면 | 현재 읽는 카드만 앞으로 이동 | P1 |
| 다시 보기 | 뽑은 카드의 미니 갤러리 | 순서대로 펼쳐지는 기록 띠 | P1 |
| 선택 환경음 | 종이·카드·작은 종소리 | 사용자가 켠 뒤에만 재생 | P2 |

카드 선택은 시드 기반 결정론을 유지한다. 연출 도중 새 난수를 만들거나 결과를 바꾸지 않는다.

### 6-4. 수비학 — 숫자가 상징으로 변하는 과정

| 아이디어 | 이미지 요소 | 애니메이션·재미 요소 | 우선순위 |
| --- | --- | --- | --- |
| 숫자 입력 흔적 | 입력 숫자의 작은 빛점 | 입력할 때마다 궤적에 점 추가 | P1 |
| 계산 과정 | 숫자와 선으로 된 기하 도식 | 합산 단계가 실제 순서대로 결합 | P0 |
| 최종 숫자 공개 | 1~9, 11, 22, 33 전용 원화 | 숫자 파동이 원화를 드러냄 | P0 |
| 마스터 넘버 | 이중 프레임과 금박 문양 | 11·22·33에서만 봉인 효과 | P1 |
| 두 숫자 비교 | 생명수·운명수 쌍둥이 패널 | 두 파동이 겹치는 지점 강조 | P1 |
| 공유 카드 | 숫자별 전용 세로 포스터 | 정적 이미지로 저장 가능한 구성 | P1 |

### 6-5. 성향검사 — 계측기에서 심상 정원으로

| 아이디어 | 이미지 요소 | 애니메이션·재미 요소 | 우선순위 |
| --- | --- | --- | --- |
| 진행 정원 | 다섯 요인의 추상 씨앗 | 10문항마다 한 영역이 성장 | P1 |
| 응답 피드백 | 점·선 기반 계측 그래픽 | 선택 값이 짧게 수렴하고 고정 | P0 |
| 진행률 | 투명한 띠와 5구간 표식 | 현재 요인 구간을 은은하게 강조 | P0 |
| 결과 풍경 | 5개 요인 전용 삽화 | 점수 비율에 따라 크롭·위치 변화 | P1 |
| 요인 탐색 | 각 요인의 상세 카드 | 포커스한 요인만 색이 살아남 | P1 |
| 응답 검토 | 번호가 있는 미니맵 | 미응답 위치로 부드럽게 이동 | P0 |

점수가 높을수록 더 좋은 시각 보상을 주지 않는다. 모든 점수 범위는 동등한 완성도와 아름다움을 가져야 하며 우열처럼 보이는 색상·크기 표현을 피한다.

### 6-6. 운세 — 오늘 움직이는 천구

| 아이디어 | 이미지 요소 | 애니메이션·재미 요소 | 우선순위 |
| --- | --- | --- | --- |
| 별자리 선택기 | 12개 별자리 원화 | 회전 천구와 스냅 선택 | P0 |
| 시간대 하늘 | 새벽·낮·해질녘·밤 그라데이션 | 현재 로컬 시간에 맞춰 정적 초기 상태 결정 | P1 |
| 오늘의 신호 | 별과 연결선 | 핵심 신호가 차례로 점등 | P0 |
| 행운 요소 | 색·숫자·방향의 작은 상징물 | 결과 패널에 인장 찍기 | P1 |
| 주간 흐름 | 7개 하늘 조각 | 드래그 시 하루씩 장면 이동 | P1 |
| 개인화 연결 | 저장된 출생 정보의 작은 별 | 공개 운세와 개인화 운세의 차이를 시각적으로 구분 | P1 |

시간에 따른 화면은 서버와 클라이언트가 다른 값을 그려 하이드레이션 오류가 나지 않도록 서버에서 기준 시각을 결정하거나 마운트 후 장식만 보정한다.

### 6-7. 공통 결과와 공유

- 첫 화면을 각 기능의 대형 `ResultCover`로 구성하고 대표 원화, 결과명, 한 줄 요약을 한 장면에 담는다.
- 긴 리포트는 챕터 내비게이션과 스크롤 진행 표시를 제공한다.
- 현재 읽는 챕터에 해당하는 배경 문양과 광원만 바뀌게 한다.
- 핵심 문장을 탭하면 해당 문장이 인장 프레임 안에 들어가는 ‘기억하기’ 상태를 제공한다.
- 공유 카드는 움직임 없는 정적 버전을 별도로 설계해 SNS 미리보기와 저장 이미지의 일관성을 확보한다.
- 인쇄·PDF 모드에서는 배경 효과, 3D, 고정 내비게이션을 제거하고 현재의 읽기 좋은 문서 구조를 유지한다.

## 7. 사용자 흐름

1. 홈 진입 → 현재 하늘 장면과 다섯 포털을 본다.
2. 포털 hover/focus/tap → 기능의 대표 상징과 짧은 미리보기가 반응한다.
3. 기능 선택 → 해당 세계의 색온도와 질감으로 장면이 전환된다.
4. 입력·선택 → 각 단계가 즉시 시각적 피드백을 돌려준다.
5. 결과 생성 → 0.7~1.4초의 단일 시네마틱 후 핵심 결과가 바로 읽힌다.
6. 탐색 → 챕터를 이동할수록 관련 이미지와 데이터 시각화가 점진적으로 등장한다.
7. 저장·공유 → 정적 공유 카드 또는 현재 결과 URL을 사용한다.

오류가 발생하면 연출을 중단하고 입력값과 복구 행동을 가장 먼저 보여 준다. 로딩이 길어질 때는 무한한 장식 애니메이션 대신 현재 단계를 설명하는 짧은 상태 문구와 결정된 루프를 사용한다.

## 8. 서버·클라이언트 컴포넌트 경계

### Server Component에 유지할 것

- 페이지 레이아웃, 번역 로딩, 메타데이터
- 사주·수비학·타로·운세·성향검사 결과 계산과 View Model 조합
- 본문, 카드 제목, 이미지 경로, 접근성 대체 텍스트
- 정적 `SceneShell`, `ResultCover`, 정보 패널과 정적 SVG

### Client Component로 제한할 것

- 포인터 위치, 드래그, 카드 선택, 탭, 로컬 진행 상태
- `motion/react`의 spring과 gesture가 필요한 작은 인터랙션 섬
- `localStorage`에 이미 저장하는 프로필·설문 초안의 구독
- 사용자가 명시적으로 켠 환경음 상태

Client Component에는 계산 엔진 전체나 메시지 카탈로그 전체를 넣지 않는다. 서버가 만든 타입 안전한 직렬화 데이터만 전달하고 `any`는 사용하지 않는다.

### Supabase 경계

이 디자인 단계에서는 계정, 수집 기록, 방문 연속성 때문에 Supabase를 새로 도입하지 않는다. 후속 제품 요구로 서버 저장이 필요해질 경우 사용자별 행은 `auth.uid()`에 묶고 SELECT·INSERT·UPDATE·DELETE 정책을 각각 정의한다. `service_role`은 서버 전용 작업에서만 사용하며 클라이언트 번들에 포함되는 모듈에서는 참조하지 않는다.

## 9. 이미지 자산 전략

### 9-1. 기존 자산 재사용

- P0·P1에서는 현재 119개 원화를 우선 재사용한다.
- 동일 원화에서 모바일 크롭, 데스크톱 크롭, 배경용 블러 변형을 파생해 새 원화 생성 비용을 줄인다.
- 기능 포털은 여러 원화를 한 파일로 합성하기보다 `next/image` 한 장과 CSS 마스크·광원 레이어를 조합한다.

### 9-2. 신규 자산 목록

- 기능별 홈 포털 대표 이미지 5장
- 공통 타로 카드 뒷면 2장과 테이블 배경 3장
- 사주 시간 다이얼·절기 링 2종
- 운세 천구 배경 4종
- 성향 정원 성장 단계 5종 또는 벡터 레이어 5개
- 공통 인장, 별, 먹 번짐, 금박 선 패턴의 가벼운 SVG 세트
- 결과 공유 카드용 기능별 프레임 5종

신규 AI 이미지는 기능별로 기준 시안 한 장을 먼저 승인한 뒤 같은 팔레트, 광원, 질감, 테두리 규칙으로 확장한다. 텍스트, 워터마크, 로고가 들어간 이미지는 사용하지 않는다.

### 9-3. 배포 자산 관리

- 브라우저 전달 포맷은 WebP/AVIF를 유지한다.
- 원본 PNG는 배포 경로와 분리하는 방안을 검토한다. 실제 이동 또는 제거는 백업 위치와 빌드 참조를 확인한 뒤 별도 승인받아 수행한다.
- 첫 화면 대표 이미지는 모바일 기준 200KB 이하를 목표로 하고 나머지는 지연 로드한다.
- 한 라우트의 초기 이미지 전송량은 1.2MB 이하, 결과 전체 탐색 후 누적 이미지 전송량은 일반 화면 4MB 이하를 목표로 한다.
- 이미지마다 고정 비율 또는 크기를 지정해 CLS를 방지한다.

## 10. 성능·접근성 예산

| 항목 | 목표 |
| --- | --- |
| 모바일 LCP | 2.5초 이하, 저속 네트워크에서 대표 이미지 1개만 우선 로드 |
| INP | 200ms 이하 |
| CLS | 0.1 이하 |
| 초기 JavaScript | 기존 기준 대비 페이지별 증분 gzip 35KB 이하 |
| 동시 환경 애니메이션 | 모바일 3개 이하, 데스크톱 5개 이하 |
| 3D·블러 효과 | 저사양·저동작 환경에서 정적 대체 |
| 본문 대비 | WCAG AA 이상, 작은 보조 텍스트는 기존 검증 수준 유지 |
| 터치 타깃 | 최소 44×44px |

추가 필수 조건:

- `prefers-reduced-motion: reduce`에서 자동 이동, 패럴랙스, 반복 회전을 끈다.
- 키보드 focus만으로 hover와 동일한 정보와 선택 상태를 확인할 수 있어야 한다.
- 카드 드래그에는 클릭·키보드 버튼 대안을 함께 제공한다.
- 색만으로 선택, 방향, 점수, 신뢰 계층을 구분하지 않는다.
- 캔버스나 WebGL을 필수 렌더링 경로로 사용하지 않는다. 도입이 필요하면 정적 DOM/SVG 대체와 성능 근거를 먼저 검증한다.
- 환경음은 기본 OFF이고 자동재생하지 않으며 한 번에 끌 수 있어야 한다.

## 11. 단계별 개발 계획

### Phase 0 — 시각 기준선과 프로토타입

목표: 구현 전에 가장 위험한 홈 포털과 타로 뽑기 경험을 작은 범위로 검증한다.

- 현재 홈·각 랜딩·결과 화면의 390px, 768px, 1440px 기준 스크린샷과 성능 수치를 기록한다.
- 밝은 콘텐츠 패널, 포털 카드, 결과 표지의 정적 시안을 만든다.
- 홈 포털 1개와 타로 카드 더미 1개의 동작 프로토타입을 만든다.
- 저동작, 키보드, 터치 대체 흐름을 같은 시안에서 검증한다.
- 시각 토큰과 모션 토큰의 이름·값을 확정한다.

완료 기준: 승인된 대표 장면 2개, 성능 예산, 정적 대체 화면이 존재한다.

### Phase 1 — 공통 장면 시스템

예상 수정 범위:

- `src/app/globals.css`
- `src/components/ambient/`
- `src/components/ui/`
- `src/lib/assets.ts`

작업:

- 표면, 광원, 깊이, 모션 시간, 이징 토큰을 추가한다.
- `SceneShell`, `AmbientLayer`, `ResultCover`, `ChapterNav`의 경계를 확정한다.
- 현재 `InkField`, `Reveal`, `Tilt`를 새 토큰에 연결하되 동작 계약은 유지한다.
- 이미지 로딩·오류·저동작 대체 패턴을 통일한다.
- 공통 컴포넌트의 키보드·터치·스크린리더 테스트를 추가한다.

완료 기준: 기능 페이지가 공통 장면 골격을 사용하면서도 서버 렌더 콘텐츠가 그대로 보인다.

### Phase 2 — 홈 포털과 첫인상

예상 수정 범위:

- `src/app/page.tsx`
- `src/components/home/FeatureHub.tsx`
- `src/components/home/SajuHubTrigger.tsx`
- 신규 홈 포털 컴포넌트와 대표 자산
- `messages/ko.json`, `messages/en.json`

작업:

- 다섯 기능 카드를 이미지 포털로 전환한다.
- 현재의 여덟 글자 히어로와 포털 사이에 시각적 연결을 만든다.
- hover/focus/tap 미리보기와 스크롤 진입 효과를 넣는다.
- 사주 카드의 폼 공개 및 `birth-form` 이동 계약을 유지한다.
- 첫 뷰포트의 우선 이미지와 나머지 지연 로딩을 분리한다.

완료 기준: 5초 안에 기능 차이를 시각적으로 이해할 수 있고, 390px에서 모든 포털을 키보드·터치로 사용할 수 있다.

### Phase 3 — 입력 경험 고도화

작업 순서:

1. 타로 카드 더미와 직접 뽑기
2. 사주 날짜·시간 다이얼과 계산 전환
3. 운세 회전 천구 선택기
4. 수비학 계산 궤적
5. 성향검사 진행 정원과 응답 피드백

각 기능은 독립 배포 가능한 단위로 만든다. 계산 엔진과 URL 결정론을 수정하지 않고 View Model과 인터랙션 표현만 교체한다.

완료 기준: 마우스, 터치, 키보드가 동일한 결과를 만들고 연출을 건너뛰어도 즉시 다음 단계로 진행할 수 있다.

### Phase 4 — 결과 화면 시네마틱과 탐색

예상 수정 범위:

- `src/app/r/[data]/`
- `src/app/tarot/[spread]/[seed]/page.tsx`
- `src/app/numerology/result/page.tsx`
- `src/app/psychometrics/result/page.tsx`
- `src/app/horoscope/[system]/[sign]/page.tsx`
- `src/components/report/`, `src/components/character/`

작업:

- 기능별 `ResultCover`를 추가한다.
- 기존 기둥, 차트, 카드, 요인 막대를 챕터 단위 장면으로 묶는다.
- 현재 읽는 섹션과 배경 장식을 연결한다.
- 결과 핵심 문장과 공유 카드 프레임을 추가한다.
- 인쇄·OG·저동작 상태를 별도로 검증한다.

완료 기준: 결과 첫 화면에서 핵심 결과를 바로 읽을 수 있고, 장식 때문에 본문이나 공유 기능이 가려지지 않는다.

### Phase 5 — 선택적 재미 실험

- 숨은 인장 또는 별 발견
- 오늘의 방문 흔적과 로컬 컬렉션
- 사용자가 켜는 환경음
- 연속 방문에 따른 배경의 작은 변화
- 결과 카드 확대 감상 모드

이 단계는 분석 이벤트와 사용자 테스트에서 효과가 확인된 항목만 유지한다. 핵심 기능 완료를 막거나 계정·DB를 요구하는 아이디어는 후속 제품 계획으로 분리한다.

### Phase 6 — 최적화와 출시 검증

- 사용하지 않는 원본의 배포 제외 방안을 적용한다.
- route별 이미지·JavaScript·메인 스레드 비용을 측정한다.
- 390px 실기기와 저사양 Android에서 애니메이션 프레임 드롭을 확인한다.
- 저동작, 고대비, 키보드 전용, 스크린리더, 인쇄 모드를 회귀 검증한다.
- 한국어·영어 문구 길이 차이와 이미지 크롭을 확인한다.

## 12. 우선순위와 출시 단위

### P0 — 반드시 먼저

- 공통 표면·모션 토큰
- 홈의 5개 이미지 포털
- 밝고 읽기 쉬운 콘텐츠 패널
- 타로 직접 뽑기와 카드 공개
- 사주 결과 조립 연출
- 운세 천구 선택기
- 모든 효과의 저동작·키보드·터치 대안
- 이미지 지연 로딩과 초기 전송 예산

### P1 — 완성도를 높이는 요소

- 기능별 입력 전환
- 결과 표지와 챕터 내비게이션
- 수비학 계산 변환
- 성향 정원과 결과 풍경
- 기능별 공유 카드
- 시간대에 따른 운세 배경

### P2 — 검증 후 유지할 재미 요소

- 숨은 인장, 방문 흔적, 로컬 컬렉션
- 선택형 환경음
- 연속 방문 배경 변화
- 감상 모드와 추가 파티클

## 13. 검증 계획

### 자동 검증

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm build
pnpm test:e2e
```

E2E에는 다음 흐름을 추가한다.

- 홈에서 각 포털을 키보드와 클릭으로 열 수 있다.
- 사주 포털 선택 후 입력 폼이 공개되고 기존 결과 URL로 이동한다.
- 타로 뽑기 애니메이션 전후 결과 카드와 시드가 동일하다.
- `prefers-reduced-motion`에서 모든 핵심 콘텐츠와 버튼이 즉시 보인다.
- 이미지 로딩 실패 상태에서도 제목과 행동이 남는다.
- 한국어·영어에서 포털, 버튼, 결과 표지가 잘리지 않는다.
- 390px에서 가로 스크롤과 포인터 전용 행동이 없다.

### 수동·시각 검증

- Chrome DevTools의 CPU 4배 제한과 Fast 3G에서 홈·타로·사주 결과를 확인한다.
- 390×844, 768×1024, 1440×900에서 시각 회귀 스크린샷을 비교한다.
- 키보드만으로 전체 주요 흐름을 완료한다.
- VoiceOver 또는 NVDA로 포털 이름, 선택 상태, 결과 순서를 확인한다.
- 애니메이션이 많은 장면에서 텍스트 대비와 초점 위치를 검사한다.
- Lighthouse Performance·Accessibility와 Web Vitals를 Phase 0 기준선과 비교한다.

### 제품 지표

- 홈 → 기능 선택 전환율
- 기능 랜딩 → 입력 시작률
- 입력 시작 → 결과 완료율
- 결과 화면의 50%·90% 도달률
- 공유 버튼 사용률
- 저동작 사용자에서의 완료율 차이
- 오류율, 이탈률, 모바일 INP·LCP

재미 요소는 체류시간만으로 판단하지 않는다. 결과 완료율과 재방문율이 개선되고 오류·이탈·성능이 악화되지 않을 때만 유지한다.

## 14. 위험과 대응

| 위험 | 대응 |
| --- | --- |
| 효과가 많아 핵심 CTA가 묻힘 | 한 화면 한 주인공 원칙과 시각 위계 테스트 적용 |
| 어두운 판타지 배경으로 가독성 재악화 | 밝기 높은 reading-panel과 대비 자동 검사 적용 |
| 이미지·모션으로 LCP와 INP 악화 | 우선 이미지 1개, 지연 로드, 작은 Client Island, 성능 예산 적용 |
| 저사양 모바일의 프레임 드롭 | 블러·입자 수 제한, transform/opacity 중심, 정적 대체 제공 |
| 장식이 과학적 결과를 왜곡 | 성향검사에는 중립적 계측·추상 성장 표현만 사용 |
| 서버·클라이언트 시간 불일치 | 서버 기준 시각 또는 마운트 후 장식 보정으로 분리 |
| 타로 연출이 결정론을 변경 | 카드 결과를 먼저 확정하고 애니메이션은 표현만 담당 |
| AI 이미지 스타일 불일치 | 기능별 기준 시안 승인 후 프롬프트·팔레트·광원 규칙 고정 |
| 원본 PNG로 배포 크기 증가 | 백업·참조 확인 후 원본을 배포 경로에서 분리하는 승인 단계 마련 |
| 환경음의 불쾌감·접근성 문제 | 기본 OFF, 자동재생 금지, 즉시 음소거 제공 |

## 15. 완료 정의

- 다섯 기능이 홈에서 이미지와 동작만으로도 서로 다른 경험임을 인지할 수 있다.
- 각 핵심 흐름에는 최소 하나의 의미 있는 이미지 장면과 하나의 행동 기반 애니메이션이 있다.
- 애니메이션을 끈 상태에서도 모든 정보, 선택, 결과, 오류 복구가 가능하다.
- 모바일 성능과 접근성 예산을 충족한다.
- 기존 계산 결과, URL 결정론, 로컬 프로필, 신뢰 계층 고지, 한국어·영어 지원이 유지된다.
- Server Component 중심 구조를 유지하고 인터랙션만 작은 Client Component로 격리한다.
- 타입 검사, 린트, 단위 테스트, 빌드, 핵심 E2E와 시각·접근성 검증을 통과한다.

## 16. 권장 첫 구현 묶음

첫 번째 구현은 **Phase 0 + Phase 1 + 홈 포털 1종의 수직 프로토타입**으로 제한한다. 타로 포털을 대표로 선택하면 기존 78장 원화, `Tilt`, 카드 플립을 재사용할 수 있어 적은 변경으로 이미지 밀도, 포인터 반응, 모바일 대체, LCP 예산을 한 번에 검증할 수 있다. 이 프로토타입이 승인된 뒤 같은 공통 문법을 나머지 네 포털과 결과 화면으로 확장한다.
