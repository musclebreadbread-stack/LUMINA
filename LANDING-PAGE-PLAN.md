# LUMINA 첫 화면 재구성 계획 — 살아있는 만다라

> 작성일: 2026-08-21 · 상태: 핵심 구현 완료 · 선택적 실험·출시 성능 검증은 별도 · 관련 문서: [DEVELOPMENT-PLAN.md](./DEVELOPMENT-PLAN.md)

## Context

LUMINA는 사주·타로·수비학·성향검사·운세 5개 기능을 갖춘 종합 플랫폼이다. 그런데 **첫 화면은 그렇게 보이지 않는다.**

접힌 선 위를 순서대로 보면 전부 사주다:

| 순서 | 내용 | 어느 기능? |
|---|---|---|
| 히어로 | 지금 이 순간의 사주 (여덟 글자) | 사주 |
| h1 | "태어난 순간을 초 단위로 계산합니다" | 사주 |
| 본문 | 절기·진태양시·표준시 이력 | 사주 |
| — 접힌 선 — | | |
| `FeatureHub` | 5개 기능 카드 | 전체 |
| 섹션 01 | 출생 정보 입력 | 사주 |
| 섹션 02 | 사양표 6항목 (`pillars`/`calc`/`elements`/`tenGods`/`luck`/`yearly`) | **전부 사주** |

방문자는 스크롤하기 전까지 이게 종합 플랫폼인 줄 모른다. 이건 애니메이션이 부족해서가 아니라 **정보 구조가 한 기능을 앞세우고 있어서**다.

동시에, 이 프로젝트에는 경쟁자가 갖지 못한 자산이 있다 — **자체 제작 일러스트 119장**(타로 78 + 별자리 12 + 십이지 12 + 숫자 12 + 성향 5). 화려함을 범용 그라디언트·파티클로 만들 이유가 없다. 이미 가진 것을 크게 보여주면 된다.

### 확정된 방향

| 항목 | 결정 |
|---|---|
| 비주얼 방향 | 현재 '계측기' 정체성 유지 + 증폭 (어두운 먹빛, 한자 글리프, 오행 5색) |
| 히어로 구조 | 살아있는 만다라 — 중심에 '지금 이 순간', 주위를 5개 기능이 공전 |
| 모션 스택 | **3D 도입 (Three.js / React Three Fiber)** |

### 3D 도입에 대한 단서

3D는 번들·GPU 부하·접근성 비용이 크고, 이 제품은 광고 수익 모델이라 Core Web Vitals가 곧 매출이다. 이 우려를 제시했고 사용자가 3D를 선택했으므로 그대로 진행하되, **점진적 향상 3층 구조**로 양립시킨다. 3D를 축소하지 않으면서 CWV와 색인을 지키는 유일한 방법이다.

---

## 핵심 설계 결정: 만다라는 장식이 아니라 계측기다

가장 중요한 결정 하나. **궤도의 회전 속도와 각 노드의 초기 각도를 임의로 정하지 않는다.** 서버에서 `src/engine/astro/positions.ts`의 `planetPosition`으로 오늘의 실제 행성 위치를 구해 배치한다.

- 달 노드는 빠르게, 토성 노드는 느리게 — 실제 공전 주기 비율
- 오늘 수성이 역행 중이면 해당 노드도 역행

그래서 "이 만다라는 오늘의 하늘이다"가 **참인 문장**이 된다. 화면 하단에 근거를 한 줄로 노출한다(`EvidenceTable`이 운세 근거를 노출하는 것과 같은 규율).

이게 이 계획의 중심축이다. 화려함이 신뢰도를 깎는 대신 증명하게 만든다. 이 장치가 없으면 그냥 예쁜 캔버스이고, LUMINA의 3계층 프레임워크와 충돌한다.

---

## 아키텍처: 점진적 향상 3층

```
Layer 0 ─ 서버 렌더 DOM  ← 진짜 콘텐츠. LCP 요소.
  h1 · 5개 기능 <a> 링크 · tier 뱃지 · 라이브 여덟 글자 · 근거 캡션
  크롤러 / JS 꺼짐 / 인쇄 / 스크린리더 는 이것만으로 완결된다.
  번들 증가: 0

Layer 1 ─ CSS 만다라  ← 대부분의 방문자가 실제로 보는 것
  Layer 0의 같은 DOM에 CSS transform으로 궤도 배치 + conic-gradient 링
  기존 qi-orbit · glyph-float · ink-drift 키프레임 재사용
  번들 증가: 0 (CSS만)

Layer 2 ─ R3F 만다라  ← 향상. 실패해도 아무도 모른다.
  조건 전부 충족 시에만 동적 import 후 Layer 1 위에 페이드인
  aria-hidden. 내비게이션은 끝까지 Layer 0 DOM이 담당한다.
```

**Layer 2 장착 조건** (하나라도 불충족이면 Layer 1에서 멈춘다):

- JS 활성 + `prefers-reduced-motion: no-preference`
- WebGL2 컨텍스트 생성 성공
- `navigator.hardwareConcurrency >= 4`
- 뷰포트 진입 (`IntersectionObserver`) — 첫 페인트를 막지 않는다
- 저장 데이터 모드 아님 (`navigator.connection.saveData !== true`)

이 구조의 요점: **Lighthouse와 크롤러는 Layer 0/1만 본다.** 3D는 측정 대상 밖에서 얹힌다.

---

## 3D 씬 사양

| 요소 | 내용 |
|---|---|
| 중심 | 지금 이 순간의 여덟 글자. 오행 5색 emissive 머티리얼 |
| 궤도 노드 | 5개. 각 기능 대표 일러스트를 텍스처로 쓴 두께 있는 판 |
| 라이팅 | 오행 5색 포인트 라이트 완만한 순환 |
| 배경 | instanced points 별 필드 (저비용) |
| 카메라 | 마우스 패럴랙스만. **`OrbitControls` 없음** — 스크롤 하이재킹 금지 |
| 포스트프로세싱 | **쓰지 않는다.** bloom은 예쁘지만 모바일 GPU를 죽인다. emissive + 텍스처로 대체 |
| 프레임 | 60fps 목표, 저사양 30fps 캡, 탭 비활성 시 정지 |

**텍스처는 별도 축소본이 필요하다.** 현재 `public/`의 WebP는 화면 표시용이라 3D 텍스처로 쓰기엔 크다. 5장 × 512px 정사각 축소본을 `scripts/`의 재인코딩 파이프라인에 추가한다.

노드에 쓸 대표 이미지는 이미 `FeatureHub.tsx`가 고른 것을 그대로 쓴다:

| 기능 | 자산 | tier |
|---|---|---|
| 사주 | `saju/zodiac/dragon` | cultural |
| 타로 | `tarot/cards/00` | cultural |
| 수비학 | `numerology/numbers/11` | cultural |
| 성향검사 | `psychometrics/factors/intellect` | scientific |
| 운세 | `horoscope/zodiac/leo` | entertainment → cultural (DEVELOPMENT-PLAN Phase 2-B) |

---

## 첫 화면 정보 구조 (재배치)

| # | 섹션 | 신규/변경 | 목적 |
|---|---|---|---|
| 1 | **살아있는 만다라** | 신규 | 접힌 선 위에서 "5가지"를 즉시 전달 |
| 2 | 다섯 개의 렌즈 (`FeatureHub`) | 유지·강화 | 만다라에서 본 것을 상세히 |
| 3 | **지금 이 순간** | 신규 | 오늘의 일진·달 위상·진태양시. 살아있음의 증거 |
| 4 | **어떻게 다른가 — 3계층** | 신규 | tier 뱃지가 무엇인지 첫 화면에 설명이 없다. 최대 차별점인데 |
| 5 | 무엇이 나오는가 | 재구성 | 사양표 6항목이 전부 사주 → 기능별로 |
| 6 | 시작하기 (출생 정보) | 위치 유지 | |
| 7 | 푸터 | 유지 | |

**h1 교체 주의**: 현재 h1 "태어난 순간을 초 단위로 계산합니다"는 사주 전용이지만 좋은 문구다. 플랫폼 문구로 h1을 바꾸되 **이 문장은 사주 전용 섹션의 h2로 살린다** — SEO 키워드를 버리지 않는다.

---

## 아이디어 뱅크

만다라 안팎에서 쓸 수 있는 연출들. 채택/보류를 구분해 남긴다.

### 채택

1. **천문 연동 궤도** — 위 "핵심 설계 결정". 이 계획의 중심.
2. **오행 색 순환 라이팅** — 목화토금수 5색이 `globals.css`에 이미 토큰으로 있다. 3D 라이트로 승격.
3. **호버 시 노드 전진 + tier 뱃지 노출** — 어느 기능이 어느 신뢰도 계층인지 만다라에서 바로 읽힌다.
4. **중심 여덟 글자의 실시간성** — 이미 `revalidate = 60`으로 계산 중. 시주가 바뀌는 순간 글자가 교체되는 전환을 준다.
5. **스크롤에 따른 색온도 순회** — `SceneShell`이 이미 기능별 톤 6종을 지원한다. 스크롤 위치에 따라 배경 톤이 해당 섹션 기능의 톤으로 전이. **CSS만으로 구현 가능, 번들 0.**
6. **근거 캡션** — 만다라 하단에 "2026-08-21 · 서울 · 달 처녀자리 · 수성 역행" 같은 한 줄. 장식이 아님을 증명.

### 보류 (2차 후보)

7. **타로 78장 카드 월** — 시각 임팩트는 최고지만 전송량이 크고 타로 편중 인상을 준다. 타로 랜딩(`/tarot`)에서 쓰는 게 맞다.
8. **한자 글리프 비(rain)** — 매트릭스 연상이 강해 '계측기' 톤과 충돌.
9. **커서 추종 먹 번짐** — `AmbientLayer`가 이미 정적 orb로 비슷한 역할. 중복.
10. **오디오(풍경 소리)** — 자동 재생은 이탈률을 올린다. 하지 않는다.
11. **로딩 인트로 애니메이션** — LCP를 직접 악화시킨다. 광고 모델과 정면 충돌.

### 기각

12. **밝은 대중 운세 앱 스타일 전환** — 사용자가 '현재 정체성 유지'를 선택. 또한 3계층 신뢰도 서술의 톤이 무너진다.
13. **`OrbitControls`로 만다라 자유 회전** — 모바일에서 스크롤을 먹는다. 페이지 이탈로 직결.
14. **스크롤 하이재킹 시퀀스** — 접근성·이탈률 모두 나쁘다.

---

## 단계 분할

각 단계는 독립 배포 가능하다. **1단계만으로도 이미 목적(종합 플랫폼임을 첫 화면에 전달)이 달성된다** — 3D는 그 위의 향상이다.

### 1단계 — 정보 구조 + Layer 0/1 (3D 없음)

가장 큰 가치를 가장 싸게 얻는 단계.

| 산출물 | 내용 |
|---|---|
| `src/components/home/Mandala.tsx` | 서버 컴포넌트. Layer 0 DOM + Layer 1 CSS 궤도 |
| `src/components/home/MandalaNode.tsx` | 노드 하나 = `<a>` + 이미지 + tier 뱃지 |
| `src/lib/mandalaModel.ts` | 천문 연동 배치 계산. `planetPosition` 소비. 로케일 무관 |
| `src/app/globals.css` | 궤도·링 클래스, 색온도 순회, 저동작 가드 추가 |
| `src/app/page.tsx` | 섹션 재배치, h1 교체, 사양표 기능별 재구성 |
| `messages/{ko,en}.json` | `home` 네임스페이스 26키 → 약 55키 |
| 테스트 | `src/lib/__tests__/mandalaModel.test.ts`, `e2e/landing.spec.ts` |

**규모**: 약 600 LOC + 250 LOC 테스트. 번들 증가 0.

### 2단계 — Layer 2 (R3F 만다라)

| 산출물 | 내용 |
|---|---|
| `src/components/home/MandalaScene.tsx` | `"use client"`, `dynamic(..., { ssr: false })` |
| `src/components/home/useMandalaCapability.ts` | 장착 조건 5종 판정 훅 |
| `scripts/` | 3D 텍스처 512px 축소본 생성 추가 |
| `package.json` | `three`, `@react-three/fiber`, `@react-three/drei`(트리셰이킹) |

**규모**: 약 400 LOC. 번들은 동적 청크로 격리 — 초기 로드에 포함되지 않는다.

### 3단계 — 다듬기

- 시주 전환 애니메이션, 노드 호버 디테일
- 프레임 예산 튜닝, 저사양 30fps 캡
- 시각 회귀 스냅샷 확정

---

## 검증 방법

```bash
pnpm lint && pnpm typecheck && pnpm test:cov && pnpm build && pnpm test:e2e
```

**성능 게이트 (광고 모델이므로 타협 불가)**

- Layer 2를 **끈 상태**로 모바일 Lighthouse: LCP < 2.5s, CLS < 0.1, TBT < 200ms
- Layer 2를 **켠 상태**: LCP·CLS가 위 수치에서 악화되지 않을 것. 악화되면 장착 조건을 조인다.
- 초기 JS(gzip)가 1단계 후 증가 0인지 확인. 2단계 후에도 **초기** 번들은 증가하지 않아야 한다(동적 청크).
- `three` + R3F 청크 실제 크기를 측정해 기록한다 — 추정치로 판단하지 않는다.

**기능 게이트**

- JS를 끈 상태에서 5개 기능 링크가 전부 동작 (`e2e`에서 `javaScriptEnabled: false` 컨텍스트)
- `prefers-reduced-motion: reduce`에서 canvas가 아예 마운트되지 않음
- 5개 기능 링크가 전부 접힌 선 위 DOM에 존재 (뷰포트 390×844 기준)
- canvas 없이도 `e2e/smoke.spec.ts`가 통과 — 현재 title만 확인하므로 자동 통과하지만, 링크 존재 검증을 추가한다
- 인쇄 미리보기에서 만다라가 사라지고 텍스트가 남는지 (`no-print` 규약)

**시각 회귀**: Playwright 스크린샷 320 / 768 / 1440, Layer 1 상태 기준(3D는 비결정적이라 스냅샷 대상에서 제외).

**i18n**: `src/i18n/__tests__/messages.test.ts`의 ko/en 키 패리티가 계속 통과. 키가 26 → 55로 두 배 늘어나므로 매 커밋 확인.

---

## 남은 위험

1. **3D 번들이 광고 수익 기반을 깎을 수 있다** — 사용자가 우려를 알고 선택한 사항. 3층 구조와 장착 조건으로 격리하되, 2단계 배포 전 실측 게이트를 반드시 통과시킨다. 통과하지 못하면 Layer 2를 데스크톱 전용으로 좁힌다.
2. **WebGL 컨텍스트 손실** — 탭 전환·GPU 압박으로 발생한다. `webglcontextlost` 처리로 Layer 1로 되돌린다. Layer 1이 항상 아래에 살아 있으므로 화면이 비지 않는다.
3. **h1 교체로 인한 사주 SEO 영향** — 현재 h1은 사주 키워드를 담고 있다. 사주 전용 섹션의 h2로 반드시 살린다. 배포 후 검색 노출 변화를 관찰한다.
4. **만다라가 5개를 다 보여주려다 아무것도 안 보이는 함정** — 노드는 5개로 고정하고 라벨을 상시 노출한다. 호버에만 의존하면 모바일에서 정보가 사라진다.
5. **텍스처 메모리** — 5장 512px이면 안전하다. 아이디어 뱅크 7번(타로 78장 월)을 만다라에 끌어들이지 않는다.
6. **`entertainment` tier 표기** — 운세 노드의 tier는 DEVELOPMENT-PLAN Phase 2-B에서 `cultural`로 바뀐다. 만다라에 tier를 하드코딩하지 말고 엔진 값을 읽는다. 두 계획이 만나는 지점이다.
7. **형상관리·원격 백업** — 코드·테스트 구현은 완료했지만, 현재 작업 트리의 실제 커밋과 원격 백업은 외부 저장소 권한이 필요한 별도 운영 작업이다. 커밋·푸시는 승인 없이 실행하지 않는다.
