# LUMINA 결과 해설 고도화 계획 — 근거 기반 상세 설명 시스템

> 작성일: 2026-08-21 · 상태: 핵심 구현 완료 · 외부 운영·전문가 콘텐츠 검수는 별도
> 관련 문서: [DEVELOPMENT-PLAN.md](./DEVELOPMENT-PLAN.md) · [LANDING-PAGE-PLAN.md](./LANDING-PAGE-PLAN.md)

> 아래 기능별 현황 표는 구현 전 감사 기준선을 그대로 보존한 기록이다. 현재 상태는 각 Phase의
> 코드·테스트·화면 반영까지 완료했으며, 공개 원자료의 한계·해석 문구의 사람 검수·운영 계정
> 연결처럼 코드만으로 확정할 수 없는 항목은 문서 하단의 위험 목록과 최종 보고에서 분리한다.

## Context

감사 결과 한 줄 요약: **이 코드베이스는 계산은 정직하고 설명은 빈약하다.**

계산 쪽은 동종 서비스 대비 매우 엄밀하다 — 사주 436케이스 교차검증, 점성술 0.05° 정밀도, 진태양시·균시차·절입 시각을 수치로 그대로 노출, 없는 규준을 지어내지 않는 정직함. 그런데 그 계산이 **무엇을 뜻하는지, 왜 그렇게 나왔는지, 어디서 온 방법인지**는 거의 설명하지 않는다.

### 기능별 현황 (코드 실측)

| 기능 | 해설 총량 | 결정적 공백 |
|---|---|---|
| **성향검사** | 5요인 합쳐 **196자** (요인당 39자) | 규준·백분위·신뢰도·측정오차 전무. `rawSum`은 계산 후 버림. `cronbachAlpha` 98 LOC 데드코드. 출처(Goldberg 1992)는 소스 주석에만. **홈이 "백분위 리포트"를 광고하는데 결과 화면은 "백분위가 아닙니다"라고 부정** |
| **사주** | 해설 사전 1,692자 중 화면 도달 극소 | 십신 10종 530자가 `<details>` **기본 접힘**. 십이운성 12종 중 **1종만** 렌더. 공망 해설 **0자**. 계산 근거 설명(`ReportNote` 6종)이 **예외 상황에만 조건부** — 정상 케이스 사용자는 섹션 자체를 못 봄. 신강/신약 임계값(0.45/0.55) 미노출. 고전 문헌 출처 0건 |
| **점성술** | 행성×별자리 120조합이 **22개 문자열 템플릿 합성** (`planetReadings` 평균 6.5자) | 하우스 12종 해설 **0자**. 알고리즘 출처(Meeus/VSOP87)는 테스트 코드에만 |
| **타로** | 78장 798자 (카드당 10자) | 마이너 56장이 **4수트×14위계 곱집합 자동생성** — 카드 고유 의미 0장. 화면 해설은 템플릿 2문장. 시드 앞 12자만 노출해 재현 검증 불가. 라이더-웨이트 출처 0건 |
| **수비학** | 12숫자 293자 (숫자당 24자) | 자릿수 합산 과정 미노출(1993→22 같은 단계). 피타고라스 문자표 코드에만. 마스터 넘버도 일반 숫자와 동일 분량 |
| **운세** | Phase 2 완료, **가장 충실** (`variants.ts` 1,914자, 근거표 UI 보유) | `signals.ts:82-84` 버그 — `addSignal`이 4슬롯에 같은 신호를 밀어넣어 mood/relationship/work/tip이 **전부 같은 신호의 변형**이고 근거표가 **항상 1행**. tier가 `entertainment`로 11곳 하드코딩돼 고지문이 현재 구현을 과소 서술 |

출처 표기는 5개 기능 중 성향검사만 있고, 그마저 결과 화면에는 없다.

### 확정된 방향

| 항목 | 결정 |
|---|---|
| 엄밀함의 종류 | **계층별로 다르게** — 통계적(성향검사) / 계산적(사주·점성술·운세) / 역사적(타로·수비학) |
| 규준 데이터 | **공개 학술 자료** — 백엔드 없이, 출처 명시, 한국인 규준 아님을 고지 |
| 전달 형태 | **점진적 공개 3단** — 요약 → 상세 → 방법론·출처 |

### 왜 "계층별로 다른 엄밀함"인가

"과학적·통계적 근거"는 5개 기능 중 **성향검사 하나에만** 문자 그대로 성립한다. 나머지는 LUMINA 스스로 `cultural` 계층으로 태깅했고 고지문에 "과학적으로 검증된 예측이 아닙니다"라고 써 뒀다. 여기에 백분위·점수 같은 통계 외형을 입히면 3계층 프레임워크가 무너지고 고지문이 거짓이 된다.

대신 각 계층이 **정직하게 줄 수 있는 최대의 엄밀함**을 준다:

| 계층 | 엄밀함의 종류 | 구체적으로 |
|---|---|---|
| `scientific` (성향검사) | **통계적** | 규준 대비 백분위·T점수, 요인별 Cronbach α, 측정표준오차(SEM), 신뢰구간, 원논문 인용 |
| `cultural` (사주·점성술·운세) | **계산적** | 알고리즘 출처(VSOP87, 절기 계산법), 판정 규칙과 임계값 전문 공개, 계산 단계 추적, 유파 간 차이 명시 |
| `cultural` (타로·수비학) | **역사적** | 원전 출처(Waite 1910, Pythagorean tradition), 전통별 해석 차이, 상징 체계의 계보 |

**공통 원칙 — "근거 없는 문장은 없다."** 모든 해설 문장은 계산 결과의 특정 값에서 나와야 하고, 그 연결을 사용자가 확인할 수 있어야 한다. 운세 엔진의 `signalId` → `evidence` 불변식을 5개 기능 전체로 확장하는 것이 이 계획의 뼈대다.

---

## 공통 아키텍처: 근거 추적 해설 시스템

### 설계 원칙

1. **해설은 데이터다, 코드가 아니다.** ko/en 짝을 가진 구조화된 사전. 엔진은 로케일을 모른다는 계약(DEVELOPMENT-PLAN 계약 3) 유지.
2. **모든 해설 문장은 `evidenceRef`를 가진다.** 어느 계산값에서 나왔는지 기계적으로 추적 가능. 운세의 `ReadingLine.signalId`와 같은 규율.
3. **깊이는 3단으로 펼친다.** 닫힌 상태의 길이는 지금과 같거나 짧게.
4. **출처는 구조화된 인용이다.** 자유 텍스트가 아니라 `{ authors, year, title, venue, url? }` 객체. 기능별 참고문헌 섹션이 자동 생성된다.

### 공통 모듈

```
src/engine/shared/
  citation.ts          ★신규  Citation 타입 + 인용 포맷터 (APA 7 한/영)
  explanation.ts       ★신규  ExplanationBlock 타입 — { summary, detail, method, evidenceRefs, citations }

src/components/ui/
  ProgressiveBlock.tsx ★신규  3단 점진 공개. <details> 2중첩, 인쇄 시 전부 펼침
  CitationList.tsx     ★신규  참고문헌 섹션 렌더
  EvidenceChip.tsx     ★신규  해설 문장 옆 "근거 보기" — 클릭 시 해당 계산값으로 스크롤·하이라이트
  MethodNote.tsx       ★신규  "이 값은 어떻게 계산됐나" 상시 노출 블록 (조건부 아님)
```

### `ExplanationBlock` 타입

```ts
export interface ExplanationBlock {
  readonly id: string;
  readonly summary: LocalizedText;        // 1단: 한 문단, 항상 보임
  readonly detail: LocalizedText;         // 2단: 펼침
  readonly method: LocalizedText | null;  // 3단: 방법론·한계. null이면 3단 없음
  readonly evidenceRefs: readonly string[]; // 이 문장이 근거하는 계산값 id
  readonly citations: readonly Citation[];
  readonly tier: EvidenceTier;            // 이 블록의 주장이 속한 계층
}
```

`tier`를 블록 단위로 다시 두는 이유: 한 리포트 안에서도 "진태양시 보정 방법"(계산적, 검증 가능)과 "편관이 많으면 ○○한 경향"(문화적, 검증 불가)은 다른 신뢰도다. 블록마다 정직하게 표기한다.

### 점진적 공개 3단 UI 규약

```
[1단 · 항상] 요약 한 문단 + EvidenceChip
[2단 · 펼침 "자세히"] 상세 해설 + 관련 수치
[3단 · 펼침 "방법과 출처"] 계산 방법 · 한계 · 유파 차이 · 참고문헌
```

- 인쇄/PDF는 **전부 펼친 상태**로 (`@media print`에서 `details[open]` 강제). 리포트를 저장하는 사용자는 깊이를 원한다.
- 스크린리더: `<details>`/`<summary>` 네이티브 시맨틱 그대로. ARIA 추가 없음.
- 펼침 상태를 저장하지 않는다. 매번 닫힌 상태로 시작 — 첫 화면 길이 예측 가능.

---

## Phase A — 성향검사: 통계적 엄밀함 (최우선)

유일하게 통계가 실제로 성립하는 기능이고, 현재 간극이 가장 크다. "계층 1 · 과학적 검증" 배지가 붙어 있는데 그 배지를 뒷받침하는 지표가 하나도 화면에 없다.

### A-0. 선행: 거짓 광고 제거

`src/app/page.tsx:58`이 렌더하는 `messages/ko.json:121` `"percentile": { "term": "백분위 리포트", "desc": "연속 점수와 자기성찰 질문" }` — 결과 화면에 백분위도 자기성찰 질문도 없다. Phase A가 완료되기 전까지는 이 문구를 현재 상태에 맞게 고치거나 내린다. **완료 후 복구.**

### A-1. 규준 데이터 확보 — 공개 원자료에서 직접 산출

**확인된 공개 자료** (2026-08-21 조사):

| 자료 | 내용 | 용도 |
|---|---|---|
| **Open-Source Psychometrics Project** `IPIP-FFM-data-8Nov2018.zip` | 같은 50문항 IPIP-FFM 응답 **1,015,342건**. 익명, 연구 목적 사용 동의 후 수집. 25편 이상 논문에 사용 | 규준 M/SD·백분위 분포 **직접 산출** |
| **ipip.ori.org** 공식 채점키 | 요인별 10문항 Cronbach α: 외향성 **.87** / 우호성 **.82** / 성실성 **.79** / 정서안정성 **.86** / 지성 **.84** | 신뢰도 표기 + SEM 계산 입력 |
| Goldberg (1992) *Psychological Assessment* 4(1), 26–42 | 척도 원논문 | 1차 인용 |
| Gow, Whiteman, Pattie & Deary (2005) *Personality and Individual Differences* 39(2), 317–329 | 스코틀랜드 3표본 N=906, 내적 일관성·동시 타당도 | 교차 검증용 2차 규준 |

**접근 방식: 빌드 타임 산출, 런타임 조회.**

```
scripts/build-norms.ts   ★신규
  1. 원자료 CSV 로드 (저장소 밖. 라이선스 불명확하므로 원자료는 커밋하지 않는다)
  2. 품질 필터 (응답 시간 이상치, 동일 응답 패턴, 결측) — 필터 기준을 산출물에 기록
  3. 요인별 rawSum 분포 → M, SD, 백분위표 (1~99), 연령대×성별 층화
  4. 산출물: src/engine/psychometrics/data/norms.json  (커밋한다 — 통계 요약값은 원자료가 아니다)
     + norms.meta.json (표본 크기, 필터 기준, 산출 일자, 원자료 해시)
```

**왜 타인의 규준을 인용하지 않고 직접 산출하는가**: 백만 건 원자료가 공개돼 있으므로 방법을 투명하게 공개한 직접 산출이 논문 한 편의 표를 옮기는 것보다 강한 근거다. 그리고 **한국어 IPIP-50 타당화 연구는 조사 시점에 확인되지 않았다** — 한국어 BFI 타당화(720명)는 다른 척도다. 이 사실은 화면에 명시한다.

**원자료 라이선스**: openpsychometrics.org는 "익명·연구 동의" 수집과 25편 이상의 학술 사용을 밝히지만 명시적 라이선스 문구는 없다. **원자료를 재배포하지 않고 통계 요약값만 산출·커밋**하며, 출처를 명시한다. 착수 전 사이트 이용약관 재확인.

### A-2. 엔진 확장

`src/engine/psychometrics/scoring.ts`의 `FactorScore`에 추가:

```ts
readonly norm: {
  readonly zScore: number;           // (rawSum − M) / SD
  readonly tScore: number;           // 50 + 10z
  readonly percentile: number;       // 규준 분포에서의 백분위 (1~99)
  readonly normGroup: "all" | "age-gender";  // 어느 규준표를 썼는가
  readonly sampleSize: number;
} | null;                            // 규준 미적용 시 null — 지어내지 않는다
readonly reliability: {
  readonly alpha: number;            // 공식 IPIP 게시값
  readonly sem: number;              // SD × √(1 − α)
  readonly ci95: readonly [number, number];  // rawSum ± 1.96·SEM
};
readonly consistency: {
  readonly withinFactorSD: number;   // 요인 내 10문항 응답 분산 — 일관성
  readonly midpointRate: number;     // "보통" 응답 비율 — 중앙 편중 경고
};
```

`reliability.ts`의 `cronbachAlpha`는 **이 앱에서는 영원히 호출할 수 없다** — n=1이라 구조적으로 계산 불가. 공식 게시 α를 상수로 쓰고, `cronbachAlpha` 함수는 `scripts/build-norms.ts`에서 원자료에 대해 실행해 **게시값과 교차검증**하는 용도로 살린다. 데드코드가 품질 게이트가 된다.

### A-3. 결과 화면 — 요인별 3단 블록

**1단 (항상)**: 요인명 · T점수 · 백분위 · 신뢰구간 막대(점이 아니라 구간) · 한 문단 요약
**2단 (자세히)**: 행동 경향 서술(고/저 각 150~250자) · 맥락별 함의(관계·일·학습) · 요인 내 응답 일관성 · 역채점 문항 수와 의미
**3단 (방법과 출처)**: 규준 표본 설명(N, 출처, 한국인 규준 아님) · α와 SEM의 뜻 · 신뢰구간 해석법 · Goldberg(1992) 인용

### A-4. 문항 수준 투명성

URL `?r=`에 50자리 응답이 이미 있다. `psychometricsModel.ts:35-53`이 이걸 요인 5개로 접어버린다. 추가 계산 없이 렌더만으로 가능한 것:

- 요인별 "가장 강하게 동의/비동의한 문항" 2개씩
- 역채점 문항 하이라이트 (정서안정성은 10문항 중 8개가 역채점 — 사용자가 모르면 혼란)
- 중앙값(3) 편중 경고 — 전체 50문항 중 40% 이상이 "보통"이면 "결과 해석에 주의" 고지

### A-5. 프로파일 해석

5요인을 각각 보는 것에서 **조합**으로. 예: 높은 성실성 + 낮은 정서안정성 조합의 알려진 패턴. 단, **조합 해석은 문헌 근거가 있는 것만** — 각 조합 문장에 `citations` 필수.

### A-6. 자기성찰 질문

홈이 광고한 "자기성찰 질문"을 실제로 만든다. 타로 엔진의 위치별 `prompt` 패턴을 빌린다 — 요인별, 점수 구간별 질문 1개. 진단이 아니라 성찰 유도.

---

## Phase B — 사주·점성술: 계산적 엄밀함

### B-1. 상시 방법론 블록 (`MethodNote`)

현재 `ReportNote` 6종은 전부 조건부라 정상 케이스 사용자는 계산 근거 섹션을 아예 못 본다. **상시 노출 블록을 추가**한다:

- 연주·월주는 절기로, 일주는 율리우스 적일로, 시주는 진태양시로 — **왜** 그렇게 하는지
- 경도 보정과 균시차가 무엇인지 (현재 수치만 있고 설명 없음)
- 절기 시각을 태양 황경으로 직접 구한다 (변환표 아님) — `astronomy-engine` 명시
- 계산 정확도 — "`lunar-javascript`(寿星天文历 계보) 대비 436케이스 100% 일치" 같은 **검증 결과를 사용자에게 공개**

점성술도 동일: 지심 겉보기 황경·트로피컬·진분점 of date·광행차 반영을 이미 `calcNote`에 쓰고 있으나 **구현 출처(VSOP87/Meeus)와 검증 정확도(0.05°)는 테스트 코드에만 있다.** 꺼내서 보여준다.

### B-2. 판정 규칙 전문 공개

신강/신약 판정은 비율(`supportRatio`)과 득령·득지·득세 on/off까지 보여주지만 **임계값 0.45/0.55가 화면에 없어 47%와 44%가 왜 다른 판정인지 알 수 없다.** 3단 블록의 방법론에 임계값·"일간 자신 −1" 처리·득세의 과반 정의를 전부 적는다. 득령/득지/득세 정의는 현재 `title` 호버 전용 → 본문으로.

### B-3. 해설 사전 완성과 노출

| 대상 | 현재 | 목표 |
|---|---|---|
| 십신 10종 | 530자, `<details>` 기본 접힘, 개인화 없음 | 요인별 3단 블록. **사용자 사주에 실제로 있는 십신만** 1단에, 전체 사전은 3단에. 각 십신 200~300자 |
| 십이운성 12종 | 616자, 현재 대운 1종만 렌더 | 원국 4기둥·대운·세운에 찍힌 **모든** 십이운성에 `EvidenceChip` → 해당 해설 |
| 공망 | **0자** | 개념·산출법(순중공망)·해석 전통별 차이. 200자 이상 |
| 대운 개별 | 공통 1문장 | 각 대운의 십신·십이운성 조합 해설. 조합 폭발 방지는 운세 엔진의 인수분해 패턴 재사용 |
| 점성술 하우스 12종 | **0자** | 12개 각 150자 이상 + 홀사인 체계 선택 이유 |
| 행성×별자리 120조합 | 22개 문자열 템플릿 | 행성 10개 × 120~200자 + 별자리 12개 × 150자로 **인수분해 확장**. 120개 전부 손으로 쓰지 않는다 — 단 의미가 큰 조합(태양·달·상승궁 × 12 = 36개)은 직접 저작 |

### B-4. 고전 문헌 인용

사주: 『淵海子平』·『三命通會』·『滴天髓』·『窮通寶鑑』 — 어떤 판정이 어느 문헌의 전통을 따르는지. **"유파마다 다르다"를 지금은 말로만 하는데, 어느 유파인지 명시**한다.
점성술: Ptolemy *Tetrabiblos* (하우스·각 전통), Meeus *Astronomical Algorithms* (계산), VSOP87 (Bretagnon & Francou 1988).

### B-5. 사문 정리

`saju.stageLabels` 12키(ko.json:246-259)는 어디서도 쓰이지 않는다. 삭제.

---

## Phase C — 운세: 버그 수정과 tier 정합

이미 가장 충실한 기능이지만 **두 가지가 설계 의도를 무력화**하고 있다.

### C-1. `addSignal` 슬롯 분화 버그

`src/engine/horoscope/signals.ts:76-85`:
```ts
for (const slot of SLOTS) {
  target.push(Object.freeze({ id, slot, score, evidence: item }));
}
```
신호 하나를 4슬롯 전부에 **같은 점수로** push하므로 `rankSignals(signals, slot)[0]`이 슬롯 무관하게 같은 신호를 고른다. 결과: mood/relationship/work/tip이 전부 같은 signalId의 문체 변형이고, `index.ts:153-158`의 dedupe 후 근거표가 항상 1행.

**수정**: 신호 정의에 슬롯별 가중치를 둔다 — 달 신호는 mood에 1.0, work에 0.4 식으로. `signals.ts`의 신호 16종 각각에 `slotWeights: Record<Slot, number>` 추가. 그러면 슬롯마다 다른 신호가 이기고 근거표가 2~4행이 된다. **이게 고쳐져야 Phase 2-7의 "분포·비퇴화" 테스트가 의미를 가진다.**

### C-2. tier 하드코딩 11곳 → `cultural`

초기 `grep`에서 발견한 운세 tier 하드코딩은 현재 모두 제거했다. 엔진 `computeDailyReading`의 `cultural` 값을 공개 운세·개인화 운세·통합 리포트·홈 만다라까지 전달하고, 각 화면은 `tier={view.tier}` 또는 `DAILY_READING_TIER`를 사용한다. 실제 천체 위치를 사용하지 않는 레거시 `computeDailyHoroscope`에는 별도의 `entertainment` 계약을 유지한다.

### C-3. 근거표 강화

신호 점수(0.9, 0.84)와 **왜 이 신호가 이겼는지**를 3단에 노출. 천문 값에는 `astronomy-engine` 출처와 UTC 정오 기준 선택 이유.

---

## Phase D — 타로·수비학: 역사적 엄밀함

### D-1. 타로 — 마이너 56장 개별 저작

현재 `buildMinorArcana()`가 4수트×14위계 곱집합으로 생성해 카드 고유 의미가 0장이다. 완드 14장이 전부 같은 키워드를 공유한다.

- `CardDef`에 `meaning: { upright: LocalizedText, reversed: LocalizedText }` 추가. 78장 각 150~250자.
- 출처: Waite, A. E. (1910). *The Pictorial Key to the Tarot*. 퍼블릭 도메인. 카드별 Waite 원문 요지를 3단에.
- 전통별 차이: 마르세유 vs 라이더-웨이트 vs 토트 — 해석이 갈리는 카드는 명시.
- **위치 × 카드 상호작용**: 켈틱크로스 10포지션 이름·질문은 이미 있다. "이 자리에 이 카드"는 조합 폭발(78×10)이므로 저작하지 않고, 위치 의미와 카드 의미를 **병치**하되 둘의 접점을 성찰 질문으로 남긴다. 현 톤 정책 유지.
- **재현성 검증 가능하게**: 시드 전문 + Fisher-Yates 셔플 설명 + "같은 시드 = 같은 카드" 검증 링크. 현재 시드 앞 12자만 노출.

### D-2. 수비학 — 계산 과정 전면 공개

- 자릿수 합산 **단계별 표시**: 1993 → 1+9+9+3 = 22 → (마스터, 정지) / 8+7+6 = 21 → 2+1 = 3. `reduce.ts`가 중간값을 반환하도록 확장.
- 피타고라스 문자-숫자 대응표 화면 노출 + 사용자 이름의 각 글자가 어떤 값으로 변환됐는지.
- 12숫자 각 150~250자, 마스터 넘버는 **왜 축약을 멈추는지** 전통적 근거.
- 생명수와 운명수 해설 **분리** (현재 같은 값이면 같은 문구).
- 출처: 피타고라스 학파 전통(Iamblichus *De Vita Pythagorica*), 현대 수비학 성립(Balliett 1900s). 「과학이 아니라 상징 체계」임을 1단에 명시.

---

## Phase E — 횡단: 참고문헌·용어집·검증 공개

### E-1. 기능별 참고문헌 페이지

`/references` (또는 각 리포트 하단). `Citation` 객체에서 자동 생성. 인용 가능한 학술 레퍼런스가 현재 사용자 화면에 **0건**이다.

### E-2. 용어집

십신·십이운성·하우스·아스펙트·T점수·SEM 등 전문 용어 `/glossary`. 해설 본문의 용어에 `<abbr>` + 링크.

### E-3. 검증 결과 공개 페이지

`/methodology` — 사주 436케이스 교차검증, 점성술 8,030건 0.05°, 일진 73,414일 전수 일치(DEVELOPMENT-PLAN 2-7), 규준 산출 방법과 표본. **테스트 코드에 있는 품질 증거를 사용자에게 보여준다.** 이게 "계층 2 · 문화적 해석" 배지의 "계산은 정확하다" 절반을 증명하는 유일한 방법이다.

---

## 아이디어 뱅크

### 채택 (위 Phase에 반영됨)

1. 규준 직접 산출 (A-1) · 2. 신뢰구간 막대 (A-3) · 3. 문항 수준 투명성 (A-4) · 4. 상시 방법론 블록 (B-1) · 5. 임계값 전문 공개 (B-2) · 6. 검증 결과 공개 페이지 (E-3) · 7. `EvidenceChip` 근거 점프 (공통) · 8. 계산 단계 추적 (D-2)

### 보류 (2차)

9. **결과 비교 — "이 사주는 얼마나 드문가"** — 오행 분포·십신 조합의 이론적 발생 확률을 60갑자 조합 공간에서 계산. 통계처럼 보이지만 실제로는 조합론이라 `cultural` 블록으로 정직하게 태깅 가능. 흥미롭지만 우선순위 낮음.
10. **성향검사 재검사 비교** — localStorage에 이전 결과를 두고 검사-재검사 변화를 신뢰구간과 함께 표시. 측정오차 개념을 체감시키는 최고의 교육 장치. Phase A 완료 후.
11. **점성술 하우스 체계 선택** — 홀사인 외 Placidus 추가. 공개 기준값 미확보로 현재 미도입(`noteHouseSystem`). 기준값 확보 시.
12. **타로 카드별 이미지 도상 해설** — 자체 제작 78장 일러스트의 상징 요소 설명. 이미지 생성 프롬프트(`tarot-image-prompts.md`)에 이미 도상 의도가 있다.
13. **사주 유파 전환 토글** — 야자시/자정론, 신강약 판정 기준 유파별 전환. 현재 `dayBoundaryRule` 옵션이 엔진에 이미 있다.

### 기각

14. **사주·타로에 백분위·점수 부여** — 근거 없는 수치. 고지문과 모순.
15. **AI 생성 해설** — 근거 추적 불가. "모든 문장은 `evidenceRef`를 가진다" 원칙 위반.
16. **자체 규준 수집** — 백엔드 필요. DEVELOPMENT-PLAN "백엔드 없음" 결정과 충돌.
17. **의학·임상 용어 사용** — `disclaimerScientific`이 "임상 평가를 대체하지 않는다"고 명시. 경계 유지.

---

## 단계 순서와 규모

| 순서 | Phase | 이유 | 규모 (추정) |
|---|---|---|---|
| 1 | **C** (운세 버그·tier) | 작고, 기존 설계 의도 복원, 다른 Phase의 패턴 기준 | 코드 ~150 LOC |
| 2 | **공통 모듈** | 이후 전 Phase가 의존 | ~400 LOC + 테스트 |
| 3 | **A** (성향검사) | 통계가 실제로 성립하는 유일한 기능. 배지와 내용의 간극 최대 | 스크립트 ~300, 엔진 ~200, UI ~400, 해설 ko/en 약 6,000자 |
| 4 | **B** (사주·점성술) | 콘텐츠량 최대 | 코드 ~500, 해설 ko/en 약 25,000자 |
| 5 | **D** (타로·수비학) | 78장 개별 저작이 가장 큰 저작 작업 | 코드 ~300, 해설 ko/en 약 20,000자 |
| 6 | **E** (횡단) | 앞 Phase의 `Citation`이 모여야 의미 있음 | ~300 LOC |

**해설 저작 총량 약 50,000자 (ko 기준, en 별도)** — 현재 전체 해설 약 7,000자의 7배. 이건 코드가 아니라 **콘텐츠 작업**이고, 전문가 검수가 필요하다. 아래 위험 3번.

---

## 검증 방법

```bash
pnpm lint && pnpm typecheck && pnpm test:cov && pnpm build && pnpm test:e2e
```

**근거 정합 불변식 (전 기능 공통, 핵심 게이트)**
- 모든 `ExplanationBlock.evidenceRefs`가 실제 계산 결과의 값 id를 가리킨다. 끊어진 참조 0건.
- 모든 `citations`가 `Citation` 스키마를 통과하고 URL이 있는 경우 형식 검증.
- 운세: C-1 수정 후 4,380 샘플에서 근거표 행 수 **중앙값 ≥ 2**. 현재는 항상 1.

**통계 정합 (Phase A)**
- `scripts/build-norms.ts`가 산출한 M/SD를 Gow et al.(2005) 게시값과 비교 — 표본이 다르므로 일치를 요구하지 않되 **방향과 규모가 맞는지** 확인. 어긋나면 산출 파이프라인을 의심한다.
- `cronbachAlpha`로 원자료에서 계산한 α가 공식 게시값(.87/.82/.79/.86/.84)과 ±0.05 이내.
- 백분위 단조성: rawSum 증가 → 백분위 비감소.
- T점수 역산: `tScore → rawSum` 왕복 오차 < 0.5.

**콘텐츠 게이트**
- ko/en 키 패리티(`messages.test.ts`) 통과 — 해설이 7배로 늘어나므로 매 커밋.
- 톤 정책: 운세의 금지어 검사(단정·공포 어휘)를 **전 기능 해설로 확장**.
- 커버리지: 십신 10·십이운성 12·하우스 12·타로 78·수비학 12·요인 5 — 각 사전의 모든 항목에 `summary`와 `detail`이 비어 있지 않음.
- 최소 분량: 항목별 `detail` ≥ 150자(ko). 미달 항목 목록을 CI가 출력.

**UI 게이트**
- 3단 블록 전부 닫힌 상태에서 각 결과 페이지의 첫 화면 길이가 **현재보다 길어지지 않음** (스크린샷 높이 비교).
- 인쇄 미리보기에서 3단 전부 펼쳐짐.
- `<details>` 키보드 조작 가능.

---

## 남은 위험

1. **원자료 라이선스** — openpsychometrics.org에 명시적 라이선스가 없다. 원자료를 재배포하지 않고 통계 요약만 산출하는 것으로 위험을 줄였지만, 착수 전 이용약관을 다시 확인하고 필요하면 운영자에게 문의한다. 확인 전에는 Phase A-1을 시작하지 않는다.
2. **한국인 규준이 아니다** — 공개 원자료는 영어권 온라인 응답자 중심이다. 이 한계를 1단 요약에 짧게, 3단에 상세히 명시한다. 감추면 "과학적"이라는 주장이 거짓이 된다.
3. **해설 콘텐츠 5만 자의 품질** — 코드로 검증할 수 있는 건 "근거 참조가 끊기지 않았는가"와 "분량·톤"뿐이다. **문장이 맞는가**는 사람이 봐야 한다. 사주는 명리 전문가, 성향검사는 심리측정 전공자 검수를 권한다. 검수 없이 배포하면 정확한 계산 위에 부정확한 해설을 얹는 꼴이 된다.
4. **Phase B 해설의 유파 문제** — 사주 해석은 유파마다 다르다. 한 유파를 골라 명시하는 것이 "모든 유파를 뭉뚱그리는 것"보다 정직하지만, 고른 유파에 동의하지 않는 사용자가 있을 것이다. 13번 아이디어(유파 토글)가 장기 해법.
5. **운세 C-1 수정이 기존 골든 픽스처를 깬다** — 슬롯 가중치 도입으로 출력 구조가 바뀐다. 픽스처 재생성이 필요하고, 그 전에 "바뀐 출력이 설계 의도대로인지" 사람이 확인해야 한다.
6. **길이와 이탈률** — 3단 구조로 닫힌 길이를 지켰더라도, 펼치는 사용자가 적으면 저작 비용 대비 효과가 낮다. 광고 모델에서 체류시간은 이득이지만 실측 전까지는 가정이다. Phase A 배포 후 펼침 비율을 측정한다(DEVELOPMENT-PLAN 7-5 관측 도입 선행).
7. **DEVELOPMENT-PLAN Phase 0 미완** — 여전히 미커밋·미백업 상태다. 5만 자 콘텐츠를 얹기 전에 반드시 형상관리를 복구한다.

---

## 참고 (조사 출처)

- IPIP 공식 채점키·α: https://ipip.ori.org/newBigFive5broadKey.htm
- Open-Source Psychometrics Project 원자료: https://openpsychometrics.org/_rawdata/ (`IPIP-FFM-data-8Nov2018.zip`, N=1,015,342)
- Goldberg, L. R. (1992). The development of markers for the Big-Five factor structure. *Psychological Assessment*, 4(1), 26–42.
- Gow, A. J., Whiteman, M. C., Pattie, A., & Deary, I. J. (2005). Goldberg's 'IPIP' Big-Five factor markers: Internal consistency and concurrent validation in Scotland. *Personality and Individual Differences*, 39(2), 317–329.
- 한국어 BFI 타당화 (IPIP-50 아님, 참고용): 간편형 한국어 BFI 타당화 연구, N=720 — KCI ART001559751
